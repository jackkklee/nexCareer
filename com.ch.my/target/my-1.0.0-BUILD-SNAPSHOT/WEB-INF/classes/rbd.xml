<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rbdns">
	<!-- Use type aliases to avoid typing the full classname every time. -->
	
	<resultMap id="rbdResult" 		type="rbd">
		<result property="rno" 			column="rno" />
		<result property="bno" 		column="bno" />
		<result property="replytext" 		column="replytext" />
		<result property="replyer" 		column="replyer" />
		<result property="regdate" 		column="regdate" />
		<result property="updatedate" 	column="updatedate" />
		<result property="del" 	column="del" />
		
		<!-- <result property="startRow" 			column="startRow" />
		<result property="endRow" 			column="endRow" /> -->
		
	</resultMap>
	<select id="list" parameterType="int" resultMap="rbdResult">
		select * from repboard where bno=#{bno} order by rno
	</select>
	<insert id="insert" parameterType="rbd">
		<selectKey keyProperty="rno" order="BEFORE" resultType="int">
			select nvl(max(rno),0)+1 from repboard
		</selectKey>
		insert into repboard values (#{rno}, #{bno},
		#{replytext}, #{replyer}, sysdate,sysdate,'n')
		
	</insert>
	<update id="delete" parameterType="int">
		update repboard set del = 'y' where rno=#{rno}
	</update>
	<update id="update" parameterType="rbd">
		update repboard set replytext=#{replytext},
			updatedate=sysdate
			where rno=#{rno}
	</update>
</mapper>